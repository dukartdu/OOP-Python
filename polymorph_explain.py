name = "Jim" #str
print(len(name))

some_list = ["some", "some"] #list
print(len(some_list))

#thats polymorphism in action, a single funcition does know
#how to handle different kinds of objects as expected!

